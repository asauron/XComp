program {boolean j int i
  int factorial(float n) {
      if (n > 2) then 
         { return 1; }
      else 
         {return n*factorial(n-1) }
  }
  while (1==1) {
      i = write(factorial(read()))
  }
}